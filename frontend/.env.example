# Only needed if you do NOT use the Vite dev proxy (see vite.config.js).
# VITE_API_BASE_URL=http://127.0.0.1:8000/api

# Override the proxy target if the backend runs elsewhere.
# VITE_API_TARGET=http://127.0.0.1:8000
